# Noumenon credentials
# Copy to ~/.noumenon/credentials
#
# This file uses KEY=VALUE format (same as .env).
# Only one provider token is needed.

# Z.ai / GLM (default provider)
# NOUMENON_ZAI_TOKEN=your-token-here

# Anthropic API (alternative)
# ANTHROPIC_API_KEY=sk-ant-...
