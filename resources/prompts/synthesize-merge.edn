{:template
 "You are finalizing the architecture of the \"{{repo-name}}\" codebase ({{file-count}} files total). Components were identified per directory partition. Your job is to consolidate them into the final architecture.

## Directory Tree

{{directory-tree}}

## Import Graph (resolved file-to-file edges)

{{import-graph}}

## Components from Partitions

{{partition-components}}

## Your Task

Consolidate the partition-level components into a final architecture:

1. **Merge** components that clearly represent the same logical unit across directories
2. **Rename** components for consistency (short kebab-case identifiers)
3. **Resolve dependencies** — replace placeholder references with final component names
4. **Assign layers and categories** from the full-codebase perspective

## Output Format

Respond with ONLY a raw EDN map (no markdown fences, no explanation).

**IMPORTANT**: Do NOT repeat file lists. Instead, use `:source-components` to map each final component back to the partition-level component names whose files belong to it. The system will reconstruct file assignments automatically.

{:components [{:name \"component-name\"
               :summary \"What it does (1-2 sentences)\"
               :purpose \"Why it exists\"
               :layer :core|:subsystem|:driver|:api|:util
               :category :backend|:frontend|:infrastructure|:configuration|:data|:testing|:documentation|:tooling|:shared
               :patterns [:pipeline]
               :complexity :trivial|:simple|:moderate|:complex|:very-complex
               :subsystem \"coarser-grouping\"
               :source-components [\"partition-component-name-1\" \"partition-component-name-2\"]
               :depends-on [\"other-final-component\" ...]}
              ...]}

## Rules

- Every partition component must appear in exactly one final component's `:source-components`
- Components should group 2-30 partition components typically
- Dependencies based on actual import edges only
- Prefer fewer, well-defined components over many small ones"

 :version "0.2.0"}
