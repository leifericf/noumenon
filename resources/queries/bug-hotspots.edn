{:name "bug-hotspots"
 :description "Files ranked by number of fix commits"
 :federation-safe? true
 :query [:find ?path (count ?commit)
         :in $ %
         :where
         (file-kind-commit ?file :fix ?commit)
         [?file :file/path ?path]]
 :uses-rules true}
