| severity | priority | fix_complexity | category | area | summary | repro | notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| High | High | Easy | Core Functional Correctness | mcp | `get-or-create-conn` calls side-effecting `db/connect-and-ensure-schema` inside `swap!` — concurrent callers with the same cache key each create a connection, leaking all but the last | Send two concurrent MCP requests for the same repo before either completes. Both `swap!` calls read the old map without the key, both execute `connect-and-ensure-schema`, and only one connection ends up in the cache — the other is leaked, holding Datomic Local file handles. | `src/noumenon/mcp.clj:50-55`. `swap!` retries on CAS failure; calling I/O inside swap! is a Clojure anti-pattern. Fix: use a separate `locking` block or `delay`-based lazy init outside the swap!. |
| High | High | Easy | Reliability/Consistency | mcp | MCP auto-sync in `with-conn` is not serialized per-repo — two concurrent stale requests both call `sync-repo!` | Send two simultaneous MCP tool calls for the same repo after a new commit. Both check `(sync/stale? db repo-path)` = true and both call `sync-repo!`, which retracts-then-reimports concurrently, racing on Datomic writes. | `src/noumenon/mcp.clj:134-139`. The `stale?` check and `sync-repo!` are not guarded by a lock. Minimum impact: double work and log noise; worst case: concurrent retract-and-reimport interleaving corrupts the sync state. |
| High | High | Easy | Core Functional Correctness | query/benchmark | `run-named-query` 2-arity always reports all inputs missing for parameterized queries — silently feeds error strings as context to LLM in benchmark and CLI | Run `noumenon query file-imports <repo>` without `--param`. Returns `{:error "Missing required inputs: [:file-path]"}`. In benchmark, `query-context` calls `(run-named-query db query-name)` for parameterized questions (q30–q32), so the LLM receives `"Query error: Missing required inputs: [:file-path]"` as its context instead of the actual import list, producing wrong answers and wrong scores. | `src/noumenon/query.clj:92`: `(remove (set (keys params)) (:inputs query-def))` where `params=nil`. `(keys nil)` = `nil`; `(set nil)` = `#{}`; `remove` removes nothing; all inputs are reported missing. Affects every 2-arity call site: `benchmark/query-context` and any CLI `query` invocation without `--param`. |
| Normal | High | Trivial | Reliability/Consistency | mcp | `handle-sync` uses the raw (non-canonical) `repo_path` string for `derive-db-name`, while all other handlers canonicalize via `with-conn` — same physical repo accessed through different string forms (`/repo`, `/repo/`, symlink) gets different Datomic databases | Call `noumenon_sync` with `repo_path="/foo/bar/"` and later call `noumenon_query` with `repo_path="/foo/bar"`. `handle-sync` passes the trailing-slash form to `derive-db-name` (producing a different hash suffix than the canonical form), while `with-conn` canonicalizes before calling `derive-db-name`. The query operates against a different (possibly empty) database. | `src/noumenon/mcp.clj:192-211`: `handle-sync` calls `validate-repo-path!` and `derive-db-name` with `(args "repo_path")` directly, not `(.getCanonicalPath (io/file ...))`. Compare `with-conn` at line 128 which canonicalizes before any use. Fix: extract a `canonical-repo-path` binding in `handle-sync` as done in `with-conn`. |
| Normal | Medium | Easy | Core Functional Correctness | cli | `parse-simple-args` unconditionally assocs `:subcommand` onto error maps, masking the error type for downstream dispatch | Pass `import --unknown-flag /path`. `parse-command` returns `{:error :unknown-flag :flag "--unknown-flag"}`. `parse-simple-args` appends `:subcommand "import"` to this error map, so any code that checks `(contains? result :subcommand)` treats it as a success path. | `src/noumenon/cli.clj:541`: `(assoc result :subcommand sub)` fires for both success and error results in `parse-simple-args`. Currently harmless because `run` checks `:error` before dispatching on `:subcommand`, but it is fragile. The `query` subcommand avoids this since its initial map already has `:subcommand "query"`. |

| Minor | Low | Easy | Reliability/Consistency | sync/watch | `do-sync` and `do-watch` use a single `--concurrency` flag for both postprocess workers (default 8) and LLM analyze workers (default 3) — passing `--concurrency 1` serializes fast file extraction alongside slow LLM calls | Pass `noumenon sync --analyze --concurrency 1 <repo>`. Both `:concurrency` (postprocess) and `:analyze-concurrency` (LLM) are set to 1, making the import-extraction phase run in a single thread even though it is CPU-bound and safe to parallelize. | `src/noumenon/main.clj:176-177` and `main.clj:200-201`: `(or concurrency 8)` is used for both keys. `sync-repo!` consumes `:concurrency` and `:analyze-concurrency` separately but the CLI cannot express different values for each. |
| Minor | Low | Trivial | Core Functional Correctness | longbench | `longbench/validate-experiment-config` is never called from `run-experiment!` — invalid configs (missing `:arms`, missing `:model`) fail with cryptic NullPointerException instead of a user-friendly error | Create an experiment config EDN that omits `:arms`. Call `noumenon longbench experiment --config path`. `run-experiment!` at line 528 calls `resolve-config-defaults` then immediately tries `(:arms config)` and `(:model config)` without going through `validate-experiment-config`. | `src/noumenon/longbench.clj:528-544`. `validate-experiment-config` is defined at line 138 but never called in `run-experiment!`. Only `do-longbench-experiment` in `main.clj:510` calls `longbench/load-experiment-config` but not the validator. |
| Minor | Low | Trivial | Observability/Operator Experience | longbench | `expected-sha256` is `nil` — dataset integrity is never verified; a tampered or truncated download is silently accepted | Run `noumenon longbench download`. `verify-integrity!` logs "integrity-skip" and the actual hash, asking the operator to pin it, but no check is ever performed. Future downloads of a corrupted file are accepted without warning. | `src/noumenon/longbench.clj:29`: `(def ^:private expected-sha256 nil)`. The comment says "Set after first verified download" but was never populated. This means there is no tamper-detection on the dataset file. The operator message is benign but the skip is permanent until the value is pinned. |
