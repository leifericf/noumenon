| severity | priority | fix_complexity | category | area | summary | repro | notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| High | High | Easy | Core Functional Correctness | mcp | `mcp/derive-db-name` uses unaliased `clojure.string` — runtime NamespaceNotFoundException on any MCP tool call that takes a repo path | Start MCP server with `serve`, call any tool requiring `repo_path` (e.g. `noumenon_import`). Crashes with `java.lang.IllegalStateException: Var clojure.string/replace is unbound`. | `src/noumenon/mcp.clj:20` calls `clojure.string/replace` and `clojure.string/split` via fully-qualified namespace without a `require`. The `clj-kondo` lint run already flags this: `warning: Unresolved namespace clojure.string`. Every MCP tool that resolves a repo path (all except `noumenon_list_queries`) is broken at runtime. |
| High | High | Medium | Data Integrity | sync | `changed-files` mis-parses git rename lines — renamed files silently produce a malformed path, breaking incremental sync for any repo with renames | Rename a committed file in the watched repo (e.g. `git mv old.clj new.clj && git commit`), then run `sync`. The modified-file retract fires on path `"old.clj\tnew.clj"` (a string with a literal tab), which matches nothing in Datomic. The old entity is not retracted and the new file is not imported with clean state. | `src/noumenon/sync.clj:38`: `(str/split line #"\t" 2)` splits a rename line (`R100\told\tnew`) into only two parts, binding `path` to the concatenated `"old\tnew"` string. Renames should be split on `#"\t"` with no limit, and the new path (third field) stored as `:added` while the old path goes to `:deleted`. |
| High | High | Easy | Core Functional Correctness | imports | Python and JavaScript import extractors use `read-string` on JSON subprocess output — crashes or security risk on import paths with EDN-special characters | Postprocess a Python or JS repo whose files import modules with names containing `#`, `@`, `\`, or `"` characters (e.g. TypeScript path aliases like `@myorg/utils`). The `read-string` call will throw or misparse the output. | `src/noumenon/imports.clj:132` (Python) and `src/noumenon/imports.clj:159` (JavaScript): both call `(read-string out)` or `(read-string (str/replace out #"'" "\""))` on JSON arrays produced by subprocesses. The correct fix is `(json/read-str out)` (already used by `invoke-api` in `llm.clj`). The `read-string` approach also silently evaluates tagged EDN literals, making it a code-execution vector if subprocess output is ever attacker-controlled. |
| Normal | High | Easy | Core Functional Correctness | query/benchmark | `query/run-named-query` 1-arity call always returns missing-inputs error for parameterized queries — `query-context` silently feeds error strings to LLM for benchmark questions q30, q31, q32 and CLI `query` for any parameterized query | Run `noumenon query file-imports <repo>` (no params). Returns `{:error "Missing required inputs: [:file-path]"}` instead of executing. Run `benchmark` against a repo with postprocess data: q30 answer stage receives `"Query error: Missing required inputs: [:file-path]"` as its context instead of the actual import list. | `src/noumenon/query.clj:92`: `(remove (set (keys params)) (:inputs query-def))` where `params=nil`. `(keys nil)` returns `nil`, `(set nil)` = `#{}`, so all inputs are reported missing. Affects every 1-arity call site: `main/do-query` (line 243), `benchmark/query-context` (line 47), and all deterministic scorers except `:q30` (which uses the 3-arity call). The 7 parameterized queries affected are: `file-imports`, `file-importers`, `file-history`, `file-authors`, `transitive-impact`, `test-impact`, `files-depending-on`. |
| Normal | Medium | Easy | Core Functional Correctness | mcp | MCP connection cache does not validate `db-dir` — same `db-name` from different `db-dir` values reuses the first connection | Call `noumenon_import` with `repo_path=/a/myrepo`, then call with `repo_path=/b/myrepo, db_dir=/other/path`. The second call silently reuses the first connection from `/a/myrepo`'s db-dir. | `src/noumenon/mcp.clj:29-33`. The cache key is only `db-name`; `db-dir` is ignored on cache hit. If a caller passes two different repos that share a basename (e.g. both named `ring`) from different directories with different `db-dir` overrides, the second gets the wrong connection. |
| Normal | Medium | Easy | Core Functional Correctness | mcp | MCP connection cache `get-or-create-conn` has a TOCTOU race — two concurrent calls with the same `db-name` both create connections; the first is leaked | Send two concurrent `noumenon_import` or `noumenon_ask` requests for the same repo before the first completes. Both calls pass the `(or (@connections db-name))` nil check, both call `db/connect-and-ensure-schema`, and `swap!` from the slower thread overwrites the faster thread's connection in the cache. | `src/noumenon/mcp.clj:29-33`. The check-then-act on the atom is not atomic. The leaked connection holds file handles to Datomic Local storage. With Datomic Local this is likely harmless (both connections share the same backing store) but is incorrect and non-deterministic. Fix: use `swap!` with a `get-or` idiom or `locking`. |
| Normal | Medium | Easy | Concurrency | sync/mcp | `mcp/with-conn` auto-sync is not serialized — two concurrent MCP requests for the same stale repo both call `sync-repo!` | Open two MCP connections and send simultaneous tool calls for the same repo after a new commit. Both see `stale?` = true and both call `sync-repo!`, duplicating work and potentially racing on Datomic writes during the retract-then-reimport cycle. | `src/noumenon/mcp.clj:133-138`. The `stale?` check and the `sync-repo!` call are not guarded by a lock or serialized per-repo. The connection-level atom does not help here. At minimum this causes double-import logging and wasted work; at worst the two concurrent retracts interleave with the subsequent `import-files!` transact. |

| Normal | Medium | Easy | Core Functional Correctness | benchmark | `stages-per-q` computed via `quot` gives wrong result for mixed-mode benchmarks, causing `max-questions` budget to fire at wrong point | Run `benchmark --max-questions 2 --skip-judge`. `stages-per-q` = `quot(2*N_q, N_q)` = 2 when both skip-raw and skip-judge are false but only skip-judge is set — the formula can diverge from the actual per-question stage count when deterministic-scoring questions mix with LLM-judged ones. | `src/noumenon/benchmark.clj:572`. `stages-per-q` uses `(quot total (count questions))` only when `(seq mode)` is truthy. This arithmetic is imprecise because `mode-stage-keys` returns a variable number of stages per question (deterministic questions keep their judge stage even with `skip-judge`). The actual stages-per-q could be 1, 2, 3, or 4 per question but `quot` always produces the average, causing off-by-one budget triggers. |

| Normal | Medium | Easy | Data Integrity | imports | `run-extraction` creates a zero-buffer channel when `files` is empty — blocks forever if called with an empty file list and concurrency > 1 | Postprocess a repo that has files but all are filtered out (e.g. all C/C++ with compiler unavailable, leaving `std-files` empty but `c-files` also empty). `extract-all` is called with an empty `files` vec and `concurrency 8`. `(async/chan 0)` creates an unbuffered rendezvous channel; `pipeline-blocking` writes into a full channel and blocks indefinitely. | `src/noumenon/imports.clj:423`: `(async/chan total)` where `total = (count files) = 0`. In practice `postprocess-repo!` does not short-circuit when `std-files` is empty, so `run-extraction` is always called. The fix is `(async/chan (max 1 total))` — identical to the guard already present in `pipeline.clj:13`. |
| Normal | Medium | Trivial | Observability/Operator Experience | longbench | `longbench results` column header says "Med" but the data key is `:medium` — spacing is misaligned for double-digit values | Run `noumenon longbench results` with a completed run that has both short and long buckets. The header column "Med" (5 chars) lines up with the formatted `%5.1f` value but the preceding "Short" (5) and following "Long" (4) values lose alignment when values are two digits (e.g. `100.0`). | `src/noumenon/main.clj:384-390`. The separator string between columns is always `"  "` (2 spaces) regardless of the formatted value width. The `%5.1f` format produces 5-char strings, but column headers are not padded to match. Low impact — cosmetic — but affects readability of benchmark output. |
| Normal | Medium | Trivial | Observability/Operator Experience | longbench | `longbench/verify-integrity!` always fires a warning — the hardcoded `expected-sha256` is a placeholder value that will never match the real dataset | Run `noumenon longbench download`. After the download, `verify-integrity!` logs a sha256 mismatch warning on every download because the `expected-sha256` constant is a placeholder hex string (not the real sha256 of the HuggingFace dataset). | `src/noumenon/longbench.clj:30`: `expected-sha256` = `"6380b9a1e4bba7e3bbcf3ca08b3e7e5c5a23e5a6f0fc3e9d8a1d4b0e2c6a9f17"`. The comment says "Set on first verified download" but the value was never updated. `verify-integrity!` at line 52 compares the actual sha256 against this placeholder and will always find a mismatch, logging an alarming integrity warning that is always a false positive. Operators cannot distinguish a genuine tampering event from the routine warning. |
| Minor | Low | Trivial | Data Integrity | sync | `retract-stale!` returns `(count paths)` not the actual count of entities retracted — misleads callers that check the return value | Call `sync` when some modified files were deleted between the diff and the retract (race) or were never imported. The return value claims N files were retracted but some paths had no DB entity. | `src/noumenon/sync.clj:99`. The function returns `(count paths)` unconditionally, even for paths that had no matching entity (the `when-let` in the inner lambda returns `nil` for them, producing no tx-data but still counted). `retract-deleted-files!` has the same issue at line 116. The return value is currently unused by callers so there is no observable bug, but the public API contract is wrong. |
| Minor | Low | Easy | Core Functional Correctness | mcp | MCP server `derive-db-name` is a private duplicate of `main/derive-db-name` — divergence risk if logic changes | Both functions apply the same logic (strip trailing slashes, split on `/`, take last). If one is updated the other is not. | `src/noumenon/mcp.clj:19-20` vs `src/noumenon/main.clj:21-24`. The mcp version additionally uses the unaliased `clojure.string` (see Bug 1). Even if Bug 1 is fixed, keeping two copies is a maintenance hazard. |
| Minor | Low | Trivial | Core Functional Correctness | cli | `parse-simple-args` does not set `:subcommand` when `parse-command` returns an error map | Pass `import --unknown-flag /path`. `parse-command` returns `{:error :unknown-flag :flag "--unknown-flag"}`. `parse-simple-args` calls `(assoc result :subcommand sub)` only for non-query subcommands, which works for the success path but also applies to error maps, silently adding `:subcommand` to error results — harmless today but fragile if error dispatch ever checks for `:subcommand`. | `src/noumenon/cli.clj:417-425`. Affects `import`, `status`, `analyze` subcommands. The `query` subcommand does not call `(assoc result :subcommand sub)` at all, so it relies on the initial map in `query-command-spec` having `{:subcommand "query"}`. |
| Minor | Low | Trivial | UX/Operator Experience | sync/watch | `do-sync` and `do-watch` use the single `--concurrency` flag for both postprocess workers (default 8) and analyze workers (default 3) — no way to independently tune both | Pass `--concurrency 1` to `watch`. Both `:concurrency` and `:analyze-concurrency` are set to 1, serializing postprocess extraction (normally fast and parallel-safe) alongside LLM calls. The user cannot say "8 workers for postprocess, 2 for LLM". | `src/noumenon/main.clj:176-177` and `src/noumenon/main.clj:200-201`: `(or concurrency 8)` is used for both keys. The `sync-repo!` function consumes `:concurrency` (for postprocess) and `:analyze-concurrency` separately, but the CLI conflates them. |
| Minor | Low | Trivial | Maintainability | benchmark | `cond` used as a local binding name in `run-benchmark!` — shadows the `cond` macro within the `for` comprehension | Attempt to use a `cond` macro expression inside the `(for [q questions, cond conditions] ...)` body. It would use the bound value rather than the macro, producing a ClassCastException. | `src/noumenon/benchmark.clj:863`: `(for [q questions, cond conditions] [(:id q) cond q])`. The local name `cond` shadows `clojure.core/cond`. The current body `[(:id q) cond q]` does not invoke the macro so there is no runtime failure today, but any future code added to the body that calls `(cond ...)` will silently receive the string value rather than branch on it. Rename to `condition`. |
| Minor | Low | Easy | Core Functional Correctness | git | `git/parse-record` indexes `fields` with hardcoded positions that are off-by-one when a `%B` (raw body) field contains `\x00` bytes — commits with null bytes in the message body produce shifted field reads | Import a git repo where any commit message contains a null byte (rare but valid UTF-8 in binary patches committed as messages). `(str/split text #"\x00" -1)` splits on every null, so a message with an embedded null produces an extra field, shifting `numstat` to `nth fields 9` instead of the correct offset. | `src/noumenon/git.clj:80-92`. The format string uses `%x00` as a field separator AND embeds the `%B` body between two `%x00` delimiters (`%x00%B%x00`). If the raw body itself contains `\x00`, the split produces more than 10 fields and `numstat` (field index 9) reads part of the message instead of the actual numstat block. In practice commit message null bytes are rare, so this is a low-probability edge case, but the parser is not robust against it. The fix is to use a record-level delimiter that cannot appear in a commit message (e.g. `%x01` already used as the record separator) or to limit the split depth. |
